apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: gnone-critical
value: 1000000
globalDefault: false
description: "Highest priority for GNONE API and critical infrastructure pods."
