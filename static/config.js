// AgentOS Configuration
window.AGENTOS_CONFIG = {
    // API Base URL - set to specific URL (e.g., "http://api.example.com") or "" for relative path
    API_BASE: "http://localhost:8000",

    // WebSocket Port - used for real-time updates (default: 8766)
    WS_PORT: 8766
};
